<UserControl x:Class="CarPaintingProcess.Views.ElectroDepositionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CarPaintingProcess.Views"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <!--수위-->
        <Label Content="1. Water Level" Grid.Row="0" FontSize="16" />
        <Label Content="수위" Grid.Row="0" FontSize="32" Margin="10,20,0,0"/>
        <Border Grid.Row="0" Width="996" Height="191">
            <lvc:CartesianChart Grid.Row="0" Series="{Binding WaterLevelData}" Margin="0,60,0,0">
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Title="Time" LabelFormatter="{Binding XTime}">
                        <lvc:Axis.Separator>
                            <lvc:Separator Step="1"/>
                        </lvc:Axis.Separator>
                    </lvc:Axis>
                </lvc:CartesianChart.AxisX>
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis Title="%" LabelFormatter="{Binding YWaterLevel}"/>
                </lvc:CartesianChart.AxisY>
            </lvc:CartesianChart>
        </Border>
        

        <!--점도-->
        <Label Content="2. Viscosity" Grid.Row="1" FontSize="16"/>
        <Label Content="점도" Grid.Row="1" FontSize="32" Margin="10,20,0,0"/>
        <Border Grid.Row="1" Width="996" Height="191">
            <lvc:CartesianChart Grid.Row="1" Series="{Binding ViscosityData}" Margin="0,60,0,0">
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Title="Time" LabelFormatter="{Binding XTime}">
                        <lvc:Axis.Separator>
                            <lvc:Separator Step="1"/>
                        </lvc:Axis.Separator>
                    </lvc:Axis>
                </lvc:CartesianChart.AxisX>
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis Title="cP" LabelFormatter="{Binding YViscosity}"/>
                </lvc:CartesianChart.AxisY>
            </lvc:CartesianChart>
        </Border>

        <!--pH-->
        <Label Content="3. pH" Grid.Row="2" FontSize="16"/>
        <Label Content="pH" Grid.Row="2" FontSize="32" Margin="10,20,0,0"/>
        <Border Grid.Row="2" Width="996" Height="191">
            <lvc:CartesianChart Grid.Row="2" Series="{Binding pHData}" Margin="0,60,0,0">
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Title="Time" LabelFormatter="{Binding XTime}">
                        <lvc:Axis.Separator>
                            <lvc:Separator Step="1"/>
                        </lvc:Axis.Separator>
                    </lvc:Axis>
                </lvc:CartesianChart.AxisX>
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis Title="pH" LabelFormatter="{Binding YpH}"/>
                </lvc:CartesianChart.AxisY>
            </lvc:CartesianChart>
        </Border>

        <!--전류-->
        <Label Content="4. Voltage" Grid.Row="3" FontSize="16"/>
        <Label Content="전류" Grid.Row="3" FontSize="32" Margin="10,20,0,0"/>
        <Border Grid.Row="3" Width="996" Height="191">
            <lvc:CartesianChart Grid.Row="3" Series="{Binding VoltageData}" Margin="0,60,0,0">
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Title="Time" LabelFormatter="{Binding XTime}">
                        <lvc:Axis.Separator>
                            <lvc:Separator Step="1"/>
                        </lvc:Axis.Separator>
                    </lvc:Axis>
                </lvc:CartesianChart.AxisX>
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis Title="V" LabelFormatter="{Binding YVoltage}"/>
                </lvc:CartesianChart.AxisY>
            </lvc:CartesianChart>
        </Border>

        <!--전압-->
        <Label Content="5. Current" Grid.Row="4" FontSize="16"/>
        <Label Content="전류" Grid.Row="4" FontSize="32" Margin="10,20,0,0"/>
        <Border Grid.Row="4" Width="996" Height="191">
            <lvc:CartesianChart Grid.Row="4" Series="{Binding CurrentData}" Margin="0,60,0,0">
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Title="Time" LabelFormatter="{Binding XTime}">
                        <lvc:Axis.Separator>
                            <lvc:Separator Step="1"/>
                        </lvc:Axis.Separator>
                    </lvc:Axis>
                </lvc:CartesianChart.AxisX>
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis Title="A" LabelFormatter="{Binding YCurrent}"/>
                </lvc:CartesianChart.AxisY>
            </lvc:CartesianChart>
        </Border>

    </Grid>
</UserControl>
